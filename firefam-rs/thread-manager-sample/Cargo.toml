[package]
name = "firefam-thread-manager-sample"
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true, features = ["derive"] }
serde_json = { workspace = true }
# Keep this sample limited to a single Firefam workspace dependency.
# Add new Firefam surface area to `firefam-core-api` instead of depending on
# additional `firefam-*` crates here.
firefam-core-api = { workspace = true }
tracing = { workspace = true }

load("//:defs.bzl", "firefam_rust_crate")

firefam_rust_crate(
    name = "exec",
    crate_name = "firefam_exec",
    test_shard_counts = {
        "exec-all-test": 8,
    },
    test_tags = ["no-sandbox"],
)

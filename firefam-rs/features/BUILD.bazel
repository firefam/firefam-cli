load("//:defs.bzl", "firefam_rust_crate")

firefam_rust_crate(
    name = "features",
    crate_name = "firefam_features",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)

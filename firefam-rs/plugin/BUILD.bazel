load("//:defs.bzl", "firefam_rust_crate")

firefam_rust_crate(
    name = "plugin",
    crate_name = "firefam_plugin",
    compile_data = glob(
        include = ["**"],
        exclude = [
            "**/* *",
            "BUILD.bazel",
            "Cargo.toml",
        ],
        allow_empty = True,
    ),
)
